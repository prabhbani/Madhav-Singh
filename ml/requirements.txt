# Training and inference. Pinned to minimums that carry the security fixes;
# the lock for a deployment comes from the container image build.
fastapi>=0.118,<1.0
uvicorn[standard]>=0.38,<1.0
pydantic>=2.12,<3.0
scikit-learn>=1.6,<2.0
pandas>=2.2,<4.0
numpy>=2.1,<3.0
scipy>=1.14,<2.0
joblib>=1.4,<2.0

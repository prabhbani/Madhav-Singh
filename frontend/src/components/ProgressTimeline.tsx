export function ProgressTimeline({ currentStage }: { currentStage: string }) { const stages = ['Project initiation', 'Survey & demarcation', 'Notice issuance', 'Objection review', 'Compensation approval', 'Award & possession']; const current = stages.findIndex((stage) => stage.toLowerCase().includes(currentStage.toLowerCase().split(' ')[0] ?? '')) >= 0 ? stages.findIndex((stage) => stage.toLowerCase().includes(currentStage.toLowerCase().split(' ')[0] ?? '')) : 3; return <div className="stage-timeline">{stages.map((stage, index) => <div className={`stage ${index < current ? 'done' : index === current ? 'current' : ''}`} key={stage}><span className="stage-bullet">{index < current ? '✓' : index + 1}</span><div><strong>{stage}</strong><small>{index < current ? 'Completed' : index === current ? 'Active · 34 days' : 'Upcoming'}</small></div></div>)}</div>; }

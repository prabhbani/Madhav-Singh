import { Filter, Search } from 'lucide-react';
import type { Risk } from '../types/project';
export function FilterBar({ query, onQuery, risk, onRisk, district, onDistrict, department, onDepartment, districts, departments }: { query: string; onQuery: (value: string) => void; risk: string; onRisk: (value: string) => void; district: string; onDistrict: (value: string) => void; department: string; onDepartment: (value: string) => void; districts: string[]; departments: string[] }) { return <div className="table-tools"><div className="search-box"><Search size={16} /><input value={query} onChange={(event) => onQuery(event.target.value)} placeholder="Search project, ID, district..." /></div><div className="select-wrap"><Filter size={14} /><select value={risk} onChange={(event) => onRisk(event.target.value)}><option value="ALL">All risk levels</option>{(['CRITICAL', 'HIGH', 'MEDIUM', 'LOW'] as Risk[]).map((item) => <option key={item} value={item}>{item[0] + item.slice(1).toLowerCase()}</option>)}</select></div><select className="compact-select" value={district} onChange={(event) => onDistrict(event.target.value)}><option value="ALL">All districts</option>{districts.map((item) => <option key={item}>{item}</option>)}</select><select className="compact-select" value={department} onChange={(event) => onDepartment(event.target.value)}><option value="ALL">All departments</option>{departments.map((item) => <option key={item}>{item}</option>)}</select></div>; }

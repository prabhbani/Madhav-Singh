import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import type { DashboardData } from '../types/project';
export function DelayTrendChart({ data }: { data: DashboardData['trend'] }) { return <><div className="chart-key"><span><i className="line-key blue" />Average probability</span><span><i className="line-key red" />Critical cases</span></div><ResponsiveContainer width="100%" height={198}><AreaChart data={data} margin={{ left: -20, right: 8, top: 15, bottom: 0 }}><defs><linearGradient id="probFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#2369a8" stopOpacity={0.18} /><stop offset="100%" stopColor="#2369a8" stopOpacity={0} /></linearGradient></defs><CartesianGrid vertical={false} stroke="#e7ecef" /><XAxis dataKey="month" axisLine={false} tickLine={false} tick={{ fill: '#76828b', fontSize: 11 }} /><YAxis domain={[0, 1]} tickFormatter={(value) => `${value * 100}%`} axisLine={false} tickLine={false} tick={{ fill: '#76828b', fontSize: 11 }} /><Tooltip formatter={(value, name) => name === 'probability' ? [`${(Number(value) * 100).toFixed(0)}%`, 'Average probability'] : [value, 'Critical cases']} /><Area type="monotone" dataKey="probability" stroke="#2369a8" strokeWidth={2.5} fill="url(#probFill)" /><Area type="monotone" dataKey="critical" stroke="#aa3140" strokeWidth={2} fill="none" /></AreaChart></ResponsiveContainer></>; }

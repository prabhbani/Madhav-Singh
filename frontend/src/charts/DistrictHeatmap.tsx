import type { DashboardData } from '../types/project';
import { Map, ChevronRight } from 'lucide-react';
export function DistrictHeatmap({ districts }: { districts: DashboardData['districts'] }) { return <><div className="heat-list">{districts.map((district) => <div className="heat-item" key={district.name}><div className="heat-label"><span className={`district-mark level-${district.value > 70 ? 'critical' : district.value > 50 ? 'high' : district.value > 30 ? 'medium' : 'low'}`}>{district.name.slice(0, 1)}</span><div><strong>{district.name}</strong><small>{district.state} · {district.projects} projects</small></div><b>{district.value}</b></div><div className="heat-track"><i style={{ width: `${district.value}%`, background: district.value > 70 ? '#aa3140' : district.value > 50 ? '#c4552d' : district.value > 30 ? '#d79a41' : '#55a477' }} /></div></div>)}</div><div className="map-note"><Map size={15} /> Highest concentration: <strong>{districts[0]?.name ?? '—'} district</strong><ChevronRight size={14} /></div></>; }
